// Script to add translations to the product database
// This is a utility script and is not used by Next.js at runtime

const fs = require('fs');
const path = require('path');

// This script is for local development only
// The product database is managed through the UI in the browser
